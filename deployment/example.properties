# paths
directory.local.base=/var/opt/powertac/
directory.host.base=${EXPERIMENT_SCHEDULER_ROOT}

# mysql
spring.datasource.username=powertac_experiment_scheduler
spring.datasource.password=${MYSQL_PASSWORD}
spring.datasource.url=jdbc:mysql://${MYSQL_HOST:mysql}:3306/powertac_experiment_scheduler